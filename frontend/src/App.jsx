import Layout from "./components/Layout";
import "./App.css";

function App() {
  return (
    <Layout>
      {/* Set the page with {currentPage === "HOME" && <Home className="mb-auto" />} */}
    </Layout>
  );
}

export default App;
