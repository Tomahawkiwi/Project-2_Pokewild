import "./App.css";
import Layout from "./components/Layout";

function App() {
  return (
    <Layout>
      {/* Set the page with {currentPage === "HOME" && <Home className="mb-auto" />} */}
    </Layout>
  );
}

export default App;
